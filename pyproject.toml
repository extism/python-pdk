[project]
name = "python-pdk"
version = "0.1.5"
description = "Extism Python PDK"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[tool.uv]
dev-dependencies = [
    "ruff>=0.6.3",
]
